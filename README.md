# SkinSync - Smart Skincare Recommendation and Safety System

SkinSync is a full-stack Flask web app that helps users build safe and personalized skincare routines.

## Setup

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 run.py
```

Open http://127.0.0.1:5000
